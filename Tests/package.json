{
  "name": "nestmap-tests",
  "version": "1.0.0",
  "description": "Playwright tests for Nestmap",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
